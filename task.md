# Milestone 1: Backend Foundation APIs

- [x] Create Ministries API: Implemented CRUD endpoints in `apps/backend/src/ministries`.
- [x] Build Favorites API: Fixed POST and GET routes returning 404 in `apps/backend/src/favorites`.
- [x] Build Reviews API: Allowed users to rate and review movies in `apps/backend/src/reviews`.
- [x] Build Admin Analytics endpoints: Created endpoints for fetching aggregations with time filters in `apps/backend/src/admin`.

Notes:
- The backend successfully compiles with the added modules (except for pre-existing errors in `payments` module regarding Stripe types).
